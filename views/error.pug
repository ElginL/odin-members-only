extends layout

block append stylelink
    link(rel="stylesheet" href="/stylesheets/error.css")

block content
    div.error-block
        h1= message
        h2= error.status
        pre #{error.stack}